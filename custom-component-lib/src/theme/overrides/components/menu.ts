import palette from '../palette';

const MuiMenu = {
  styleOverrides: {
    paper: {
      borderRadius: 10,
      border: '1px solid',
      borderColor: palette.secondary[100],
      padding: 0,
    },
  },
};

export default MuiMenu;
